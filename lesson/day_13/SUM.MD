. setTimeOut(callbackFn, timeoutValue);
--

. callback -> callback hell (nested callbacks)
. Promise -> thenable
. async/await

---
Promise.all()

const resolvedValues = Promise.all([promise1, promise2])